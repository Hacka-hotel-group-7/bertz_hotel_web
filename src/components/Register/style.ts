import { styled } from "styled-components";

export const RegisterStyled = styled.main`
  padding-top: 150px;
  width: 90%;
  margin: 0 auto;

  form {
    margin-top: 25px;
    display: flex;
    flex-direction: column;
    gap: 50px;
  }
`;
